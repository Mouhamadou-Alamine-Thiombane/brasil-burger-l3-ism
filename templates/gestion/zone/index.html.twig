{% extends 'base.html.twig' %}

{% block title %}Gestion des zones - Brasil Burger{% endblock %}

{% block body %}
<div class="container-fluid">
    <div class="row mb-4">
        <div class="col-12">
            <h1 class="h3 mb-0">
                <i class="fas fa-map-marked-alt me-2"></i>
                Gestion des zones
            </h1>
        </div>
    </div>

    <div class="card">
        <div class="card-body">
            <div class="text-center py-5">
                <i class="fas fa-tools fa-3x text-muted mb-3"></i>
                <h4 class="text-muted">Section en construction</h4>
                <p class="text-muted">La gestion des zones sera bientôt disponible.</p>
                <a href="{{ path('gestion_dashboard') }}" class="btn btn-primary mt-3">
                    <i class="fas fa-arrow-left me-2"></i>Retour au tableau de bord
                </a>
            </div>
        </div>
    </div>
</div>
{% endblock %}